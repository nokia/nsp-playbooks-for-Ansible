# Ansible defaults when running playbooks from this directory (e.g. make test).
# Do not symlink to ../ansible.cfg — collection build resolves symlinks and
# requires the target to exist in the repository.
[defaults]
retry_files_enabled = False
